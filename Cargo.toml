[package]
name = "ciris-node-core"
version = "0.1.0-dev"
edition = "2021"
description = "Second-tier consensus runtime for the CIRIS federation: contributions, votes, expertise/credits ledgers, moderation, slashing, reconsideration"
license = "AGPL-3.0-or-later"
authors = ["CIRIS Team"]
repository = "https://github.com/CIRISAI/CIRISNodeCore"

[lib]
name = "ciris_node_core"
# `rlib` for the post-fold-into-agent trajectory (MISSION.md §1.4 lifecycle:
# Spec → Impl → Deployed pilot → Deployed folded). `cdylib` so safety.ciris.ai
# can consume via PyO3 during the pilot window.
crate-type = ["cdylib", "rlib"]

[dependencies]
# Substrate. Persist v1.11.0 — the live CIRIS 3.0 cohabitation
# convergence point (closes CIRISPersist#90: node_core_service() →
# NodeCoreDispatch on Engine/PyEngine, the Phase 2-3 PyO3 unblock).
# Pins CIRISVerify v2.8.0. Consumed surface (cirisnode track,
# federation trust_grant, audit service/types) byte-stable since
# v0.9.1 / v1.5.x respectively.
# `postgres` is explicit: v1.11.0 decoupled `cirisnode` from the
# backend features (`cirisnode = []`), so the `postgres` feature is
# needed for the `NodeCoreDispatch::Postgres` variant that
# `crate::cohabitation::install_from_dispatch` matches. The
# cohabitation target is a Postgres deployment; the agent process
# already links the Postgres backend via persist's Engine.
ciris-persist = { git = "https://github.com/CIRISAI/CIRISPersist", tag = "v1.11.0", version = "1", features = ["cirisnode", "cirisaudit", "postgres"] }

# Edge v0.2.8 — carries persist v1.11.0 + verify v2.8.0, single-
# versioned with our direct persist pin (federation graph converged).
# Consumed surface (`src/messages/`, `src/handler.rs`, `src/edge.rs`)
# byte-stable v0.2.5 → v0.2.8.
#
# DEPLOYMENT NOTE: persist v1.10.0 (#87) enables ciris-keyring's
# `tpm` feature per-target on Linux; cargo additive feature
# unification forces it onto every co-resident crate. Linux builds +
# any Linux host running the shared-engine cohabitation process need
# system `libtss2` (`libtss2-dev` for builds). See CIRISEdge#12.
ciris-edge = { git = "https://github.com/CIRISAI/CIRISEdge", tag = "v0.2.8", version = "0" }

# Serde + canonicalization. Canonicalization itself routes through
# persist's `Engine.canonicalize_envelope_for_signing` — see
# FSD/SUBSTRATE_INTEGRATION.md §3. Node-core never re-canonicalizes.
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

# Crypto. Ed25519 verification lives in edge/persist; node-core only
# uses it for type signatures on its wire envelopes. ML-DSA-65 PQC
# half of the hybrid signature is opaque to node-core (persist owns it).
ed25519-dalek = { version = "2.1", features = ["std"] }
base64 = "0.21"
sha2 = "0.10"

# Identifiers + time. ULID for `contribution_id`, `vote_id`, etc.
# per SCHEMA.md §2.2.
ulid = { version = "1.1", features = ["serde"] }
chrono = { version = "0.4", features = ["serde"] }

# Async + observability
tokio = { version = "1", features = ["rt-multi-thread", "macros", "sync", "time"] }
tracing = "0.1"

# Error handling
thiserror = "1.0"
async-trait = "0.1"

# PyO3 — Phase 1 client-surface bindings (CIRISNodeCore#1). Optional;
# gated behind the `python` feature. v0.28 + abi3-py311 matches
# `ciris-persist` + `ciris-lens-core` exactly — required for CIRIS 3.0
# in-process cohabitation (all co-resident cdylibs must link one
# identical pyo3 ABI).
pyo3 = { version = "0.28", features = ["extension-module", "abi3-py311"], optional = true }

[dev-dependencies]
proptest = "1.4"
uuid = { version = "1", features = ["v4"] }

[features]
default = []

# PyO3 cdylib bindings. Phase 1 = the client surface (envelope
# construction + Ed25519 signing — pure, no engine). Per
# CIRISNodeCore#4 the bindings NEVER construct a persist Engine or a
# tokio runtime; Phase 1 has no engine to inject. Phases 2-3
# (relay/node modes that host substrate) consume an injected persist
# Engine handle — gated on a persist coordination item, not in this
# feature yet.
python = ["dep:pyo3"]
